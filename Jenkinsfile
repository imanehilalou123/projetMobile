pipeline {
    agent any

    stages {

        stage('Test Git') {
                    steps {
                        echo "Jenkins lit bien le Jenkinsfile !"
                        sh 'ls -la'
                        sh 'git branch -a'
                    }
                }
    }
}
